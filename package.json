{
  "name": "discova-deploy-root",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "preinstall": "sh -c 'if [ \"$VERCEL\" = \"1\" ]; then exit 0; fi; rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "postinstall": "sh -c 'if [ \"$VERCEL\" = \"1\" ]; then cd futurestack && HUSKY=0 npm ci --engine-strict=false; fi'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "vercel-build": "npm --prefix futurestack run vercel:build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "dependencies": {
    "next": "16.2.0"
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
